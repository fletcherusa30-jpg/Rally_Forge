export const helperText = Object.freeze({
  knowledge: Object.freeze({
    designModeNotice: 'Design mode only: fully mocked, no external API calls, no billing.',
    simulatedSalePricePrefix: 'Simulated sale price per search:',
  }),
  treatment: Object.freeze({
    editingEntry: 'Editing an existing condition. Update the fields and click Update Entry.',
    manualEntryGuide: 'No document? Enter a current diagnosis or symptom record directly. Condition names are normalized to VA terminology.',
  }),
});