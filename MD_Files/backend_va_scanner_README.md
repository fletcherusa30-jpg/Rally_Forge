# VA Scanner Backend Folder

Reserved for backend VA scanner-specific modules and integration helpers.

If scanner logic is added here, keep endpoint code in `backend/api/` and shared logic in `backend/services/` or `backend/engine/`.
