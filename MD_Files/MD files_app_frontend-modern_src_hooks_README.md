# Hooks
Shared React hooks for `frontend-modern` should live in this folder.
Guidelines:
- Keep hooks framework-agnostic when possible.
- Avoid direct DOM mutation inside hooks.
- Export named hooks from dedicated files (one primary hook per file).

