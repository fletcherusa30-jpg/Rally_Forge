{
  "name": "rally-forge",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "start": "node backend/server.js",
    "predev": "node -e \"console.log('Skipping workspace build; running local dev servers.')\"",
    "dev": "concurrently -k -n web,api -c blue,green \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "http-server frontend -p 5174 -c-1",
    "dev:api": "node backend/server.js",
    "build": "node -e \"console.log('No build step configured for this repo layout.')\"",
    "test": "node -e \"console.log('No tests configured for this repo layout.')\"",
    "test:va-decision-scanner": "node --test tests/va_decision_scanner/scanner.test.js",
    "lint": "node -e \"console.log('No lint step configured for this repo layout.')\"",
    "format": "prettier --write .",
    "sep:validate": "node docs/spd_engine/validate_inputs.js",
    "sep:extract": "node docs/spd_engine/run_extract.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "http-server": "^14.1.1",
    "eslint": "^9.8.0",
    "prettier": "^3.3.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.10.0"
  }
}
